using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Assets.Scripts
{
    public class Enums
    {
        public enum Pages
        {
            Home,
            Tutorial,
            Shop,
            Score
        }

        public enum UIAnimationScale
        {
            min = 1,
            max = 2
        }

        public enum Axis
        {
            X = 1,
            Y = 2,
            Z = 3
        }
    }
}
